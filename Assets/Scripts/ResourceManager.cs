using UnityEngine;
using System.Collections;

public class ResourceManager : MonoBehaviour {

    bool loadedTileMapSprites;
	// Use this for initialization
	void Start () {
        Debug.Log("It Loaded without being attached");
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
